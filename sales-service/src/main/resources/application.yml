server:
  port: 8083

spring:
  application:
    name: sales-service
  
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:trabix_db}
    username: ${DB_USER:trabix_admin}
    password: ${DB_PASSWORD:trabix_secure_2024}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

# Configuración JWT
jwt:
  secret: ${JWT_SECRET:clave_secreta_muy_larga_para_desarrollo_local_trabix_2024_minimo_256_bits}

# PRECIOS DE VENTA (hardcodeados en VentaService):
# - UNIDAD: $8,000 (con licor)
# - PROMO: $12,000 total (2 x $6,000)
# - SIN_LICOR: $7,000
# - REGALO: $0 (máx 8% del lote)
# - MAYOR_CON_LICOR:
#   - 21-49: $4,900
#   - 50-99: $4,700
#   - 100+:  $4,500
# - MAYOR_SIN_LICOR:
#   - 21-49: $4,800
#   - 50-99: $4,500
#   - 100+:  $4,200

# OpenAPI
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

# Logging
logging:
  level:
    com.trabix: DEBUG
